# Overview

This application is an Android app that displays random dog images fetched from a public API.

Purpose:
To demonstrate API integration and image display using Kotlin and XML Views.

Target Users:
General users who want to browse random dog images.

System Behavior:
The app fetches images from an API and displays them in a scrollable list.
Users can refresh the content to load new images.